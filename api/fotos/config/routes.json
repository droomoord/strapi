{
  "routes": [
    {
      "method": "GET",
      "path": "/fotos",
      "handler": "fotos.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/fotos",
      "handler": "fotos.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/fotos",
      "handler": "fotos.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
